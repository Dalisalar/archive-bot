version: '3'

services:
  bot:
    build:
      context: .
    depends_on:
      - mongo
    environment:
      - TELEGRAM_BOT_TOKEN=TELEGRAM_BOT_TOKEN
    network_mode: "host"

  mongo:
    image: mongo
    volumes:
      - ./data:/data/db
    network_mode: "host"